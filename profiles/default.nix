{ config, lib, pkgs, ... }:

{
  # Make it so that one can install the desired DE/WM through their assigned profile names.
  # Names: Akkad = XMonad, Babylon = Waymonad, (insert japanese city here..) = LeftWM, Madurai = Gnome, Hefei = Plasma.
}
