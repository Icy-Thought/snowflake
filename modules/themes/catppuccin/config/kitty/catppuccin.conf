{ colors, font, ... }: ''

  font_family          ${font.sans.family}
  italic_font          auto
  bold_font            auto
  bold_italic_font     auto
  font_size            ${toString (font.sans.size)}

  foreground           ${colors.types.fg}
  background           ${colors.types.bg}

  cursor               ${colors.types.bg}
  cursor_text_color    ${colors.yellow}

  selection_foreground ${colors.types.fg}
  selection_background ${colors.types.highlight}

  color0               ${colors.black}
  color8               ${colors.brightBlack}

  color1               ${colors.red}
  color9               ${colors.brightRed}

  color2               ${colors.green}
  color10              ${colors.brightGreen}

  color3               ${colors.yellow}
  color11              ${colors.brightYellow}

  color4               ${colors.blue}
  color12              ${colors.brightBlue}

  color5               ${colors.magenta}
  color13              ${colors.brightMagenta}

  color6               ${colors.cyan}
  color14              ${colors.brightCyan}

  color7               ${colors.white}
  color15              ${colors.brightWhite}
''
